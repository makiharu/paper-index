node_modules/
dist/
coverage/
.DS_Store

# OCR cache and temporary runtime data stay outside this repository.
.paper-inbox/

# Original PDFs/images are retained locally but must never be committed.
originals/

# OCR results are retained locally but must never be committed.
ocr-results/
